node {
    def app
    environment {
        DB_HOST="${DB_HOST}"
        DB_USER="${DB_USER}"
        DB_PW="${DB_PW}"
        DB_PORT="${DB_PORT}"
        DB_NAME="${DB_NAME}"

        COOKIE_SECRET_KEY="${COOKIE_SECRET_KEY}"
        NODEMAILER_ID="${NODEMAILER_ID}"
        NODEMAILER_PW="${NODEMAILER_PW}"
        MAILER_HOST="${MAILER_HOST}"
        CERTIFICATION_EMAIL="${CERTIFICATION_EMAIL}"
        JWT_SECRET_KEY="${JWT_SECRET_KEY}"
    }
    stage('Clone repository') {
        git 'https://github.com/MinYeongPark/stop_bang.git'
    }
    stage('Build Image') {
        app = docker.build('minyeongpark5/stop_bang')
    }
    stage('Push Image') {
        docker.withRegistry('https://registry.hub.docker.com', 'minyeongdockerhub') {
            app.push("${env.BUILD_NUMBER}")
            app.push("latest")
        }
    }
}